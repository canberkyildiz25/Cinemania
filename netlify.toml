[build]
  command = "npm run build"
  publish = "dist"

# React Router istemci tarafında çalışıyor; derin bağlantılar da index.html'e düşmeli
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "22"
